import React from 'react';
import { Box, Typography } from '@material-ui/core';

const CasesList = () => {
  return (
    <Box>
      <Typography>Cases List</Typography>
    </Box>
  );
};

export default CasesList;
